[phases.setup]
aptPkgs = [
    "libnss3",
    "libnspr4", 
    "libatk1.0-0",
    "libatk-bridge2.0-0",
    "libcups2",
    "libdrm2",
    "libdbus-1-3",
    "libxkbcommon0",
    "libatspi2.0-0",
    "libxcomposite1",
    "libxdamage1",
    "libxfixes3",
    "libxrandr2",
    "libgbm1",
    "libasound2"
]

[phases.install]
cmds = [
    "npm install",
    "cd backend && npm install",
    "cd backend && npx playwright install chromium --with-deps",
    "cd web && npm install"
]

[phases.build]
cmds = ["cd web && npm run build"]

[start]
cmd = "npm start"
