[phases.install]
cmds = [
    "npm install",
    "cd backend && npm install",
    "cd backend && npx playwright install chromium --with-deps",
    "cd web && npm install"
]

[phases.build]
cmds = ["cd web && npm run build"]

[start]
cmd = "npm start"
