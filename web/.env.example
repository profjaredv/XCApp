# Neon Auth (Stack) — get these from your Neon project's Auth tab, or the
# Stack Auth dashboard if you set it up directly.
VITE_STACK_PROJECT_ID=your-stack-project-id
VITE_STACK_PUBLISHABLE_CLIENT_KEY=your-stack-publishable-client-key

# API Configuration
VITE_API_BASE_URL=http://localhost:3001/api
